{
  "name": "coral-img-tasks",
  "version": "1.0.0",
  "description": "Modules that creates and writes thumbnails to disk. It wraps a few functions from 'sharp' module inside Tasks (from folktale/data.task), for easy function composition (using, for example, Ramda's composeK).",
  "main": "img-tasks.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --colors --reporter spec ./**/*spec.js"
  },
  "author": "Diego Villaseñor",
  "license": "ISC",
  "devDependencies": {
    "cheerio": "^0.22.0",
    "gulp": "^3.9.1",
    "gulp-mocha": "^4.3.1",
    "gulp-watch": "^4.3.11",
    "jasmine-node": "^1.14.5",
    "chai": "^3.5.0",
    "request": "^2.79.0",
    "mocha": "^3.2.0",
    "proxyquire": "^1.7.10"
  },
  "dependencies": {
    "coral-fs-tasks": "^1.0.0",
    "data.task": "^3.1.1",
    "ramda": "^0.23.0",
    "sharp": "^0.18.2"
  }
}
